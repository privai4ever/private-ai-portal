-- Intentionally left blank.
-- Previously contained a one-off profile backfill for the original instance,
-- removed from the repo to avoid leaking personal data. No-op on all environments.
SELECT 1;
